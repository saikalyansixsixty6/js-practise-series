"use strict"; //treats all javascript code as newer version

//alert(3+3)  it throws an error because we are using nodejs, not browser
let name = "virat"; //string type
let age = 18;//number type
let isLoggedIn = true;//boolean type

/**
 * (PRIMITIVE_DATATYPES)
 * number => range between 0 to 2 power 53
 * bigInt => more than 2 power 53
 * boolean => true/false
 * string => "" (or) ''
 * null => standalone value(type of null is object)
 * undefined => where variable is created value is not assigned
 * 
 * 
 * (REFERENCE_DATATYPES)
 * 1.Arrays
 * 2.Objects
 * 3.Functions
 */

console.log(typeof 18);        //number
console.log(typeof "king");    //string
console.log(typeof true);      //boolean
console.log(typeof null);      //object
console.log(typeof undefined); //undefined

